# shengji-frontend

声纪前端共享组件库，被 GitHub 版和公司 GitLab 版共同引用。

## 目录结构
- `components/` - UI 组件
- `lib/` - 工具函数
- `types.ts` - 类型定义
- `styles.css` - 样式

## 使用方式
作为 git submodule 引入，放在各仓库的 `src/shared/` 或 `front/src/shared/` 下。
